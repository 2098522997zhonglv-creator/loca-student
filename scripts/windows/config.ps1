# Optional overrides for Windows auto-update.
# Copy values from your machine if defaults are wrong.

# Conda Python for env "loca_stude"
$script:PythonExe = "D:\conda\envs\loca_stude\python.exe"

# Service bind
$script:BindHost = "0.0.0.0"
$script:Port = 8000

# Git
$script:Remote = "origin"
$script:Branch = "main"
